name: CEAP Platform - Build and Deploy

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  JAVA_VERSION: '17'
  GRADLE_OPTS: '-Xmx3072m'

jobs:
  # ============================================================================
  # Build and Test Job
  # ============================================================================
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'gradle'
      
      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: Build with Gradle
        run: ./gradlew build -x test
      
      - name: Run unit tests
        run: ./gradlew test
      
      - name: Run property-based tests
        run: ./gradlew test
      
      - name: Generate code coverage report
        run: ./gradlew jacocoTestReport
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./build/reports/jacoco/test/jacocoTestReport.xml
          flags: unittests
          name: codecov-umbrella
      
      - name: Verify Lambda JARs
        run: |
          echo "Verifying Lambda deployment packages..."
          for module in ceap-workflow-etl ceap-workflow-filter ceap-workflow-score ceap-workflow-store ceap-workflow-reactive; do
            if [ -f "$module/build/libs/$module-1.0.0-SNAPSHOT.jar" ]; then
              echo "✓ $module JAR exists"
              ls -lh "$module/build/libs/$module-1.0.0-SNAPSHOT.jar"
            else
              echo "✗ $module JAR not found"
              exit 1
            fi
          done
      
      - name: Upload Lambda artifacts
        uses: actions/upload-artifact@v3
        with:
          name: lambda-jars
          path: |
            ceap-workflow-etl/build/libs/*.jar
            ceap-workflow-filter/build/libs/*.jar
            ceap-workflow-score/build/libs/*.jar
            ceap-workflow-store/build/libs/*.jar
            ceap-workflow-reactive/build/libs/*.jar
          retention-days: 7

  # ============================================================================
  # Code Quality Job
  # ============================================================================
  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'gradle'
      
      - name: Run Checkstyle
        run: ./gradlew checkstyleMain checkstyleTest
        continue-on-error: true
      
      - name: Run SpotBugs
        run: ./gradlew spotbugsMain
        continue-on-error: true
      
      - name: Run PMD
        run: ./gradlew pmdMain
        continue-on-error: true

  # ============================================================================
  # Deploy to Dev Environment
  # ============================================================================
  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    needs: [build, code-quality]
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    environment:
      name: dev
      url: https://dev.ceap-platform.example.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Download Lambda artifacts
        uses: actions/download-artifact@v3
        with:
          name: lambda-jars
          path: target/deploy-dev
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_DEV }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_DEV }}
          aws-region: us-east-1
      
      - name: Deploy infrastructure
        run: |
          chmod +x scripts/deploy.sh
          ./scripts/deploy.sh -e dev -r us-east-1
      
      - name: Run smoke tests
        run: |
          echo "Running smoke tests..."
          # Add smoke test commands here
      
      - name: Notify deployment
        if: always()
        run: |
          echo "Deployment to dev completed with status: ${{ job.status }}"

  # ============================================================================
  # Deploy to Staging Environment
  # ============================================================================
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [build, code-quality]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: staging
      url: https://staging.ceap-platform.example.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Download Lambda artifacts
        uses: actions/download-artifact@v3
        with:
          name: lambda-jars
          path: target/deploy-staging
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_STAGING }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_STAGING }}
          aws-region: us-east-1
      
      - name: Deploy infrastructure
        run: |
          chmod +x scripts/deploy.sh
          ./scripts/deploy.sh -e staging -r us-east-1
      
      - name: Run integration tests
        run: |
          echo "Running integration tests..."
          # Add integration test commands here
      
      - name: Monitor deployment health
        run: |
          echo "Monitoring deployment for 5 minutes..."
          sleep 300
          # Add health check commands here
      
      - name: Notify deployment
        if: always()
        run: |
          echo "Deployment to staging completed with status: ${{ job.status }}"

  # ============================================================================
  # Deploy to Production Environment
  # ============================================================================
  deploy-prod:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-staging
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://ceap-platform.example.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Download Lambda artifacts
        uses: actions/download-artifact@v3
        with:
          name: lambda-jars
          path: target/deploy-prod
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_PROD }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_PROD }}
          aws-region: us-east-1
      
      - name: Deploy infrastructure
        run: |
          chmod +x scripts/deploy.sh
          ./scripts/deploy.sh -e prod -r us-east-1
      
      - name: Run smoke tests
        run: |
          echo "Running production smoke tests..."
          # Add smoke test commands here
      
      - name: Monitor deployment health
        run: |
          echo "Monitoring deployment for 30 minutes..."
          sleep 1800
          # Add health check commands here
      
      - name: Rollback on failure
        if: failure()
        run: |
          echo "Deployment failed, initiating rollback..."
          # Add rollback commands here
      
      - name: Notify deployment
        if: always()
        run: |
          echo "Deployment to production completed with status: ${{ job.status }}"

  # ============================================================================
  # Security Scanning Job
  # ============================================================================
  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'gradle'
      
      - name: Run OWASP Dependency Check
        run: ./gradlew dependencyCheckAnalyze
        continue-on-error: true
      
      - name: Upload security report
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: security-report
          path: build/reports/dependency-check-report.html
          retention-days: 30
